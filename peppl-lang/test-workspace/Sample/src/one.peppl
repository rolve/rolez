class Object

class String {
	def pure length: int { return 0; }
}

class Person {
	val name: readonly String
	val surname: readonly String
	var address: readwrite Address
	
	def readonly fullName: readonly String {
		val n: Null = null;
		return "Hi";
	}

	def readwrite setAddress(val address: readwrite Address): void {
		this.address = address;
	}
	
	def readonly bar(val k: int): void {		
		var i: int = -555555555;
		val j: pure String = "Hi";
		
		this.address = null;
		new Account;
		
		val s: readonly String = "Hi" + -'c' + null + 5 + null + this.surname;
		
		while(i < j.length())
			if((false && !true) || 5 < 0 + 7)
				i = 2*i + 2;
			else
				i = k;
	}
}

class Student extends Person {
	val university: readonly String
	
	def pure bla: void {
		this.name;
		this.setAddress(new Address);
	}
}

class Account {
	val owner: readonly Person
	val description: readonly String
}

class Address {
	val street: readonly String
	val number: readonly String
	val city: readonly String
	val zipCode: readonly String
	
	def readonly foo(val a: readonly Account, val b: readonly Account): void {
		this.city;
		this.foo(a, new Account);
		
		val p: readonly Person = new Person;
		p.name;
		p.fullName();
		new Person.setAddress(null);
	}
}

main {
	var o: pure Object = "Hello";
	var a: readonly Address = new Address;
	a.city;
}
