package foo

class SomeImportantClass[T] {
	var foo: readwrite Account
	var i: int
	var t: T
	
	new {}
	
	def pure bar: readonly Account {
		if(true)
			return new Account;
		return null;
	}
	
	def readonly bar(a: int): T {
		this.bar;
		
		val t = this.t;
		return t;
	}
}
