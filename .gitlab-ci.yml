image: rolve/maven-with-xvfb:3-jdk-7

variables:
  MAVEN_REPO: $CI_PROJECT_DIR/maven-repo
  MAVEN_OPTS: "-Xmx1g -XX:MaxPermSize=512m"

cache:
  paths:
    - $MAVEN_REPO/

test:
  script: xvfb-run mvn install -B -Dmaven.repo.local=$MAVEN_REPO